mirror/
crawl-*.log
node_modules/
.wrangler/
.env
worker/dev.log
worker/tail.json
sync-*.log
sync-*.log.*
finish-load-*.log
finish-load-*.out
cutover-diff-*/
mirror.urls
*.urls
inventory/*.gz
mirror.urls
mirror.redirects.tsv
mirror.targets
cutover-diff-*/
.claude/handoffs/

# Leftovers from the site build that used to live here; it moved to the
# bioconductor-website repo. Ignored so a stale local copy cannot be recommitted.
.cache/
astro/
pipeline/
docs/
bioc.py

# Local-only exports (issue archives, findings). Never commit.
*.local.md
backfill-*.log
